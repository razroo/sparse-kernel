# Book Writing Swarm

This example is a placeholder for a SparseKernel workload where many logical agents draft, review, fact-check, and revise text while only a bounded active set is materialized.

Expected resource profile:

- high logical agent count;
- low browser usage;
- low sandbox usage;
- frequent small artifact writes;
- strict task leases for file updates.
